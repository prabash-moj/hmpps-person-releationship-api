spring:

  datasource:
    url: 'jdbc:h2:mem:contacts-db;MODE=PostgreSQL'
    username: contacts
    password: dummy

  flyway:
    locations: classpath:/migrations/common,classpath:/migrations/test,classpath:/migrations/h2

  h2:
    console:
      enabled: true
